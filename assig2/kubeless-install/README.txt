Kubeless setup for Linux & Windows, assuming that the kubeless executable is already installed on the system...

The 'install' script creates the kubeless namespace and its 'customresourcedefinition' if these dont already exist.
The 'remove' script deletes these if they exist.